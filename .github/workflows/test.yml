name: Python application test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python 3.11
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    - name: List installed packages (debug)
      run: pip list
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install qrcode pillow python-dotenv Flask Flask-JWT-Extended Flask-Cors gunicorn razorpay requests Werkzeug Flask-Limiter limits  
    - name: Lint with flake8 (optional)
      run: |
        pip install flake8
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
    - name: Run basic import test
      run: |
        python -c "from app import app; print('✅ App imports OK')"